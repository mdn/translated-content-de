---
title: WebDriver Classic-Befehle
short-title: Commands
slug: Web/WebDriver/Reference/Classic/Commands
l10n:
  sourceCommit: 225de04b84b717433ffeb3bb0cf5ceddac9653ea
---

WebDriver Classic-Befehle werden als HTTP-Anfragen an spezifische Endpunkte auf der entfernten Seite gesendet. Befehle werden verwendet, um das Verhalten eines Benutzeragenten zu steuern, DOM-Elemente zu entdecken und zu manipulieren sowie Browserfenster und -sitzungen zu verwalten.

Wenn ein WebDriver-Client einen Befehl sendet, gibt er die HTTP-Methode (`GET`, `POST`, `DELETE`) und den Endpunktpfad an, der der gewünschten Operation entspricht. Die entfernte Seite verarbeitet den Befehl und gibt eine HTTP-Antwort mit dem Ergebnis oder einem Fehler zurück.

## Liste der Befehle

Folgend finden Sie eine Liste der verfügbaren WebDriver Classic-Befehle:

{{ListSubPages}}

## Browser-Kompatibilität

{{Compat}}
